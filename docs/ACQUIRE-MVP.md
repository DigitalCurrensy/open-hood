# Acquire + code — remaining 8

Heaviest MVP wave. [WAVE-PLUS10.md](WAVE-PLUS10.md) thickened the honest bay **without** licensed pipes. Those ticks (UX hydrate-first, safe SW) stay. They do **not** count here.

Full gap → fix → done for every PAPER / weekend / leftover / company row: **[GAPS.md](GAPS.md)**. Paste-ready vendor letters: **[OUTREACH.md](OUTREACH.md)**. You cannot sign Chrome / Motor / Carfax / CR / TecDoc in a coding session. You can send the eight letters and paste the weekend keys.

This wave: **buy or paper the jack, then code the live path.** Adapter goes live when the key (and any counsel gate) is on. Honest dark when it is not. `configured: true` without a key is a scoring fail.

Night bay. Native `fetch` only. No cart.

**Demo VIN lock (do not regress):** `1HGCM82633A004352` → **2003 Honda Accord**, displacement **3.0L** (not `3.00`), oil **5W-20**. Door jamb still wins a catalog miss.

Overall is **61** in [RATING.md](RATING.md) (code + honesty). It stays under **100** until a signed Motor / Chrome / Carfax / TecDoc feed lands. **105% is not a score.** Weekend keys move the eight blockers below. They do not buy a factory book. Company stays **35** until users.

Scorekeeper edits [RATING.md](RATING.md) after the desks work. Not before.

---

## Founder — 48-hour weekend run

Do these first. Then code. Chrome / Motor / Carfax / Consumer Reports / TecDoc are **contracts, not weekend keys** — email them (letters in [OUTREACH.md](OUTREACH.md)); do not wait on those replies to ship the jacks you can actually turn on. Tick the send log. A sent letter is PAPER started. This session cannot close PAPER.

| Hour | Job | Done when |
| --- | --- | --- |
| **0–1** | Live origin. Waiting room hits **:3000** or production — not a leftover **:3100** tab. `scripts/rescue-3100.mjs` or `/?unlock=1`. | Cold phone opens Car → Ticket → Script. Honda demo **3.0L / 5W-20**. |
| **1–4** | Weekend keys in the table below. One plate decoder, not both. Stripe **test** only. | `/integrations`: those jacks `configured: true` only if the key is really on. Chrome / Motor / TecDoc still false. |
| **4–8** | Send every letter in [OUTREACH.md](OUTREACH.md): Chrome, Motor, Mitchell, Carfax, CR, TecDoc, PartsTech, CarsXE, MarketCheck, VinAudit. Enroll Apple Developer if you will TestFlight. | Send log has dates. Inbox has no “we already licensed Chrome.” |
| **8–12** | Counsel ask (contractor): plate DPPA yes/no; Stripe live **not yet**; escrow **no**. | Email to counsel exists. Checkbox on the plate desk stays an acknowledgment. |
| **12–24** | Ten real `/contact` asks with consent to be named. One Stripe **test** payment if `sk_test_` is on. Photograph one RO if OpenAI is on. | jsonl has N ≥ 0 honest tickets. `/trust` still `users: 0` until those people exist. |
| **24–48** | Scorekeeper. Honda lock. Licensed racks empty. [GAPS.md](GAPS.md) A rows still PAPER. | [RATING.md](RATING.md) not written to 105. Company still **35** until users. |

**Ops in the same 48 hours (not a vendor):** confirm the waiting room hits **:3000** or production — not a leftover **:3100** tab. A dead worker cannot score UX or Mobile.

## Founder — 48-hour buy list

| Buy now | Env | What it unlocks | Skip if |
| --- | --- | --- | --- |
| **OpenAI** platform.openai.com | `OPENAI_API_KEY` | Quote + VIN photo vision. `/api/ocr` stops being a dark card. | You like Tesseract lottery. Then Quote stays in the 60s — still not Motor. |
| **CarsXE** *or* **MarketCheck** — one, not both required | `CARSXE_API_KEY` or `MARKETCHECK_API_KEY` | Plate-to-VIN on `POST /api/history/plate`. Hook already exists. | Counsel has not signed DPPA / plate ToS. Then the key sits dark. That is correct. |
| **Google Places** (optional) | `GOOGLE_PLACES_API_KEY` | Nearby rooftops on `GET /api/directory/places`. OSM stays live without it. | Billing scare. OSM-only stamp is honest. |
| **Yelp Fusion** (optional) | `YELP_API_KEY` | Phones / addresses on the same Places jack. | Same. Do not show a live-looking empty board. |
| **Stripe test** dashboard.stripe.com | `STRIPE_SECRET_KEY=sk_test_…` | Test hold on `/trust`. Says test. Never captured. | You were about to paste `sk_live_`. Refuse. Live is counsel + WP-12, not 48 hours. |

**Do not buy this weekend (paper only — send the letter):**

| Vendor | Env (hook only) | Door |
| --- | --- | --- |
| J.D. Power Chrome Data | `CHROME_DATA_KEY` | jdpower.com/business/automotive/chrome-data |
| MOTOR / Identifix (Hearst) | `MOTOR_API_KEY` | motor.com |
| Mitchell / ProDemand | `MITCHELL_API_KEY` | mitchell.com/contact-us |
| TecAlliance (TecDoc) | `TECALLIANCE_KEY` | tecalliance.net/en/contact |
| PartsTech | `PARTSTECH_API_KEY` | partstech.com/contact |
| Carfax for Dealers | — | carfaxfordealers.com |
| Consumer Reports | — | Ask once ([OUTREACH.md](OUTREACH.md)). They do not sell weekend bubbles. |
| NHTSA VIN open/closed | — | SaferCar partnership / fleet vendor. Public `recallsByVin` is **403**. |
| Apple Developer | — | $99 enrollment is an account, not a store listing. [NATIVE.md](NATIVE.md). |

A key in env for Chrome / TecDoc / MOTOR / VinAudit does **not** flip `configured: true`. [`.env.example`](../.env.example) already says so. Contract first. Letters: [OUTREACH.md](OUTREACH.md). Ledger: [GAPS.md](GAPS.md) A1–A13.

---

## 1. Identification — 52

**Blocker:** plate keys off; no VIN-true file.

**Why another +10 did not land.** The identity packet can bind vPIC + fluids + a SaferCar href, but a plate is still a note and `recallsByVin` is still 403 — you cannot code a DMV or an open/closed campaign file.

**Acquire this week.**

- **Buy:** CarsXE (`api.carsxe.com`, `CARSXE_API_KEY`) **or** MarketCheck (`marketcheck.com`, `MARKETCHECK_API_KEY`). One decoder. What it unlocks: plate + state → VIN on the history plate desk.
- **Paper:** Chrome Data (`CHROME_DATA_KEY`) — window-sticker facts, not a weekend key. NHTSA VIN partnership — open/closed on *this* VIN. Email first sentences in [OUTREACH.md](OUTREACH.md).
- **Gate:** counsel reads DPPA / plate ToS before production. Key without a signature stays dark.

**Code this wave.** `POST /api/history/plate` already wired. When a plate key is on **and** counsel said yes: decode, stamp the vendor, bind the VIN into the identity packet. When either is missing: plate / state / miles stay **notes**. Chrome adapter stays `{connected:false}` — no fetch. SaferCar VIN is a link-out. Show the 403. Do not parse HTML.

**Advisor.** **Legal** — plate stays a note until DPPA / ToS is signed; then licensing turns **one** decoder on. Not both. Not Chrome.

**+10 definition.** On a live key, a US plate + state returns a VIN the owner can take to `/sticker`. Without a key, the desk says note — not “lookup failed, here’s a guess.” VIN-true campaigns remain a SaferCar href. Honda demo still **2003 Accord · 3.0L · 5W-20**.

**Hard no.** Scrape SaferCar VIN HTML. Fake a Chrome / Monroney sheet. Plate default-on. Invent a VIN from a plate.

---

## 2. Quote — 54

**Blocker:** vision key off; Tesseract lottery.

**Why another +10 did not land.** A thicker typical-hour book still leaves the real input — a waiting-room photo — to on-device OCR. Licensed hours are paper. You cannot regex your way to Motor.

**Acquire this week.**

- **Buy:** OpenAI (`platform.openai.com`, `OPENAI_API_KEY`). Unlocks quote-photo + VIN-photo vision on `/api/ocr` and the quote desk.
- **Paper:** MOTOR (`MOTOR_API_KEY`), Mitchell (`MITCHELL_API_KEY`), TecDoc (`TECALLIANCE_KEY`), PartsTech (`PARTSTECH_API_KEY`). Email Hearst / Mitchell / TecAlliance. Hours and VIN SKUs do not land in 48 hours.

**Code this wave.** Photo path: JPEG compress → vision when `OPENAI_API_KEY` is on → Tesseract fallback that **says lottery** when the key is off. Paste path stays keyless. Compare stays ZIP indie band + typical hours. Source stamp on every line: regional typical, not factory. No `motor.ts`. No TecDoc SKUs.

**Advisor.** **AI** — buy the vision key this week; do not wait on Motor to make the RO readable.

**+10 definition.** A phone photo of a real RO produces flagged lines + a three-line script when the OpenAI key is on. When it is off, the desk names Tesseract and does not pretend it read the ticket. Hours never wear a Motor / Mitchell / ALLDATA badge.

**Hard no.** Motor-labeled heuristics. Factory hours. Minted TecDoc interchange. Fake “in stock.”

---

## 3. UX — 58

**Blocker:** dead `:3100` tab if you don’t unlock.

**Why another +10 did not land.** Hydrate-first and a thin ticket bar do not help if a leftover worker still owns the tab. A dead origin cannot take a key.

**Acquire this week.**

- **Buy:** nothing licensed. Acquire a **clean origin** — `next start` / production on **:3000** (or the live host). Kill `:3100`.
- **Ops:** `scripts/rescue-3100.mjs` and `/?unlock=1` are the unlock. Founder runs them in the 48-hour window.
- **Paper:** no marketplace skin, no account wall, no Inter.

**Code this wave.** Unlock is one stamp, not a support ritual. Leftover SW: leftover-only in `next dev`; always-register in production. Never intercept `navigate` / `document`. Empty / error copy on Car / Ticket / Shops names the next desk. First-run three stamps if WAVE-PLUS10 already shipped them — do not restyle the bay into SaaS.

**Advisor.** **Mobile** — the leftover worker is a PWA defect. Decision: the live tab is `:3000` or production; `:3100` is a rescue, not a product.

**+10 definition.** A cold phone can open Car → Ticket → Script on the live origin without WI-FI DROPPED painted on every stamp. Unlock works once. A founder who skips unlock does not get to claim 68.

**Hard no.** Full-page “Opening bay…”. Cached `/offline` HTML on `/quote` `/directory` `/recalls`. Generic-SaaS homepage. Book-a-shop in the header.

---

## 4. Data — 48

**Blocker:** heuristic miss; not Carfax; not CR.

**Why another +10 did not land.** More fluids rows and louder `catalog` vs `heuristic` stamps still leave wrecks, titles, and reliability bubbles with vendors who do not sell a weekend JSON dump.

**Acquire this week.**

- **Request / buy if they sell:** VinAudit (`vinaudit.com/api`, `VINAUDIT_API_KEY`) or another NMVTIS provider (`NMVTIS_PROVIDER_KEY`). Title / salvage snapshot we actually paid for.
- **Paper — not weekend keys:** Carfax for Dealers (`carfaxfordealers.com`) — hard sale to an advocate. Consumer Reports — they will not sell you bubbles. Cox / KBB (`KBB_API_KEY`) — residuals later; `/value` stays illustration + consumer search URLs.
- **Outreach:** Carfax and VinAudit first sentences in [OUTREACH.md](OUTREACH.md). Do not email CR asking to clone the magazine.

**Code this wave.** History desk: NHTSA Y/M/M + complaints + stars stay live. Consumer Carfax / AutoCheck / NMVTIS stay **purchase URLs**. If a VinAudit (or NMVTIS) **contract** is signed and the key is on: show that snapshot and name the vendor. Key in env without a contract: `configured: false`, empty bay. Heuristic fluids stay `source: heuristic`. Honda miss must not invent 5W-30.

**Advisor.** **Data** — a paid title snapshot is the only +10 that is not a pamphlet; Carfax and CR stay paper until they sell. Do not relabel NHTSA as either.

**+10 definition.** On a contracted VinAudit / NMVTIS key, `/history` shows a title-brand snapshot for a VIN and says who sold it. Without that contract, the axis **stays 48** — more JSON rows are WAVE-PLUS10, not this wave. Reliability stays complaint counts + a published method. Not CR.

**Hard no.** Fake Carfax XML. Scrape consumer Carfax / AutoCheck HTML. Invent wrecks or odometer. CR bubbles. Heuristic labeled `catalog`.

---

## 5. Integrations — 42

**Blocker:** keys off; empty licensed catalog; Stripe live refused.

**Why another +10 did not land.** The honesty bay already groups live / outbound / dark / refused. An empty licensed rack with every jack off is still 42.

**Acquire this week.**

- **Buy:** OpenAI + one plate decoder (same 48-hour list). **Optional:** Places (`GOOGLE_PLACES_API_KEY`) and Yelp (`YELP_API_KEY`).
- **Stripe:** `STRIPE_SECRET_KEY` as **`sk_test_` only**. Live (`sk_live_`) stays refused. Not escrow.
- **Paper:** Chrome, MOTOR, Mitchell, TecDoc, Carfax, eBay Browse (`EBAY_CLIENT_ID` + `EBAY_CLIENT_SECRET`). Request-access cards stay complete. Do not flip `configured: true`.

**Code this wave.** `GET /api/integrations/status`: each jack is **live / outbound / dark / refused**. Places / Yelp: `{connected:false, results:[]}` + “OSM only” without keys; Nearby / Fusion when keys are on. Plate: live only with key + counsel. Chrome disconnected — no fetch. Stripe live refused. VIN-campaign probe may report 403 — that is a pass. `public/openapi.yaml` lists routes that exist.

**Advisor.** **Payments** — test hold this week; live charges and escrow stay no. Licensing papers the empty catalog; engineering does not mint a Motor client.

**+10 definition.** At least **two** coded jacks report **live** on a cold `/integrations` GET (vision, plate, Places, or Stripe test — whatever you actually bought). Every licensed row is still request-access or `{connected:false}`. `sk_live_` cannot look configured.

**Hard no.** `configured: true` without a key. Places/Yelp merge theater on an empty board. New Motor / Carfax clients. Escrow. Pay-the-shop.

---

## 6. Trust — 44

**Blocker:** no social proof by design.

**Why another +10 did not land.** ToS, privacy, and a refuse wall are honesty. They are not users, reviews, or a counsel stamp. Inventing those is how this score dies.

**Acquire this week.**

- **Buy:** counsel hours (contractor). Decision in writing: plate ToS / DPPA — yes or not yet; Stripe live — **not yet**; escrow / MTL — **no**.
- **Buy (money):** Stripe **test** key only. Hold says test.
- **Do not buy:** testimonials, review widgets, fake user counts, a bond number we do not have.

**Code this wave.** `/trust` refuse wall stays: no users in this repo, no reviews, we do not certify shops, counsel has not signed live charges. Source stamps the owner can read: NHTSA vs heuristic vs ZIP typical vs outbound Carfax. `sk_live_` refused. Test key says test. No fake deposit. `security.txt` / privacy stay linked.

**Advisor.** **Legal** — institutional paper is the +10. Social proof is a hard no. If counsel has not signed, the badge does not appear.

**+10 definition.** A written counsel decision is linked or quoted on `/trust` (plate: gated; live Stripe: refused; escrow: refused). Test hold, when a test key is on, says **test**. User count remains zero. Review count remains absent — not “0 reviews,” not “coming soon.”

**Hard no.** Minted reviews. Invented users. Counsel-signed badge without a signature. Escrow theater. Bond number we did not file.

---

## 7. Mobile — 36

**Blocker:** no BLE; no store app.

**Why another +10 did not land.** A safe SW and a pin are a website. Safari has no Web Bluetooth. An App Store listing is not a weekend key — it is a native app we have not started.

**Acquire this week.**

- **Buy:** one **ELM327** puck + an **Android Chrome** phone you can stand in a waiting room with.
- **Paper:** Apple and Google **developer accounts** (not a store submit). [LICENSES.md](LICENSES.md) status stays **Not started** for store apps.
- **Do not buy:** a fake “Get it on the App Store” badge. iOS BLE requires native. That is hire-4 / later, not this wave.

**Code this wave.** Chrome / Android Web Bluetooth remains the live scan path. Desktop scan stays **demo PIDs** and says so. PWA: pin this site; `/__bay/last.json` keeps last car + last script; `/offline` is its own route; SW never caches desk HTML. Install copy: “pin this site.” Never “get it on the App Store.” `/?unlock=1` still saves a dead `:3100` tab.

**Advisor.** **Mobile** — developer accounts this week; no store binary. Live path is Android Chrome + puck. iOS BLE stays honest dark.

**+10 definition.** On Android Chrome, a real puck yields live Mode 01 PIDs + Mode 03 codes, the site pins, and a killed radio still has last car + last script. iOS says it cannot pair. There is no store listing. A leftover `:3100` worker does not own the tab.

**Hard no.** iOS BLE theater. App Store / Play listing we did not ship. HTML intercept. Cached `/quote` `/directory` `/recalls`. Desktop scan labeled live.

---

## 8. Uniqueness — 76

**Blocker:** falls if we grow a cart.

**Why another +10 did not land.** 76 is already the advocate job. The missing ten is that job **executed on live pipes** — not a booking cut, not inventory, not escrow. A cart makes us them.

**Acquire this week.**

- **Buy:** the 48-hour keys (OpenAI, one plate decoder, optional Places, Stripe test). Those feed Identification, Quote, Integrations. They must not grow a checkout.
- **Refuse:** shop-network, lead-fee, escrow.com-class, CDK-write-the-RO, “Book this shop” contracts. If a vendor’s paper requires we dispatch or take a cut, walk.
- **Paper:** Motor / Chrome / TecDoc still help uniqueness later (AXIS 80). They are not this weekend, and they are still not a marketplace.

**Code this wave.** One waiting-room spine: identity packet → grease-pencil → three-line script → print/share. `/book` stays visit intake + OEM scheduler **outbound**. Stripe test hold is not Pay-the-shop. Stamp what RepairPal / Carfax / AutoZone / dealer apps optimize for vs what this bay stamps. We take no cut. They route the job.

**Advisor.** **Legal** — we will not sign escrow, MTL, or shop-dispatch paper this wave. If uniqueness work starts looking like a network, stop.

**+10 definition.** The owner can identify **this** car (VIN or, with a key, plate), mark **this** RO (vision if the key is on), and leave with three lines — on the live origin — with **no** booking CTA, **no** cart, **no** inventory. Licensed jacks that are off stay dark. If a “Book this shop” control ships, uniqueness **falls**.

**Hard no.** Book-a-shop. Escrow. Pay-the-shop. Fake inventory. Certified-shop network. A cut.

---

## Scoring rule

- +10 only if the **acquire** happened **and** the live path is coded **and** the hard-no wall held.
- Do not double-count WAVE-PLUS10 pamphlet work.
- Do not claim `configured: true` without the key (and contract, where [`.env.example`](../.env.example) says so).
- Do not call heuristic hours Motor. Do not call NHTSA a Carfax or CR file.
- Plate without a key stays a note. Chrome stays disconnected. VIN campaign probe may be 403.
- Demo VIN lock holds or the wave is a fail.

Related: [GAPS.md](GAPS.md) · [WAVE-PLUS10.md](WAVE-PLUS10.md) · [LICENSES.md](LICENSES.md) · [OUTREACH.md](OUTREACH.md) · [RATING.md](RATING.md) · [ROADMAP.md](../ROADMAP.md) · [AXIS-100.md](AXIS-100.md) · [NATIVE.md](NATIVE.md).

---

## Audit — 27 Aug 2026

Ops packet landed: [GAPS.md](GAPS.md) A–E, [OUTREACH.md](OUTREACH.md) letters, weekend run above. **No licenses bought this session.** Chrome / Motor / Carfax / CR / TecDoc stay PAPER. [RATING.md](RATING.md) stays **61 / 35** — not 105.

Axes vs this doc (code only; keys/paper stay paper):

| Axis | Status |
| --- | --- |
| 1 Identification | **PARTIAL / IN FLIGHT** — adapter + `inventedVin:false` + Chrome false + SaferCar/403 OK; live bind incomplete; `vin-identify` mid-edit |
| 2 Quote | **DONE** — OpenAI path, lottery stamp, quote-desk vision stamp |
| 3 UX | **DONE** — unlock + rescue-3100; `sw.js` no document intercept |
| 4 Data | **DONE** — fluids ~322, heuristic stamp, history≠Carfax, reliability≠CR, Honda 5W-20; VinAudit stays paper |
| 5 Integrations | **DONE** — `configured:true` only with wired keys; Stripe escrow refused; licensed catalog empty |
| 6 Trust | **DONE** — honest zeros + ticket counts; counsel memo still paper |
| 7 Mobile | **DONE** — Android BLE / iOS type-code / NATIVE.md / `sw.js` 200; store apps paper |
| 8 Uniqueness | **DONE** — compare-3 ROs, no book CTA, ticket path |

Cross-checks: `/vin/:vin` redirect OK · Honda `3.0L` OK · `public/sw.js` never intercepts `navigate`/`document`.

**Remaining code gaps (punch list):**

- `src/app/sticker/plate-desk.tsx` — live POST expects `connected`; `handlePlateDecode` omits it; never binds VIN → session/identify
- `src/app/api/history/plate/route.ts` — live success body needs `connected` + `inventedVin:false` (or wrap `handlePlateDecode`)
- `src/components/vin-identify.tsx` — **IN FLIGHT**; no POST `/api/history/plate` when `plateConnected`; OCR plate path never decodes
- `src/lib/history/plate.ts` (+ `.env.example`) — no counsel/DPPA gate; key alone flips live (ACQUIRE wants key **and** counsel)

PAPER / weekend / leftover / company process: [GAPS.md](GAPS.md). Do not close A1–A11 from code.
